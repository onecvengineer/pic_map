{
  "name": "@pic-map/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "npm run build -w @pic-map/shared && npm run build && node dist/main.js",
    "serve": "node dist/main.js",
    "parse": "node dist/cli.js"
  },
  "dependencies": {
    "@fastify/multipart": "^9.2.1",
    "@fastify/static": "^8.2.0",
    "@pic-map/shared": "0.1.0",
    "exiftool-vendored": "^35.18.0",
    "fastify": "^5.6.0",
    "heic-convert": "^2.1.0",
    "undici": "^8.1.0"
  }
}
